<div class="smalltron">
  <h1>Walls <%= link_to "+ New", new_wall_path, :class => "btn btn-primary" %></h1>

</div>


<ul class="thumbnails">
  <% @walls.each do |wall| %>

  <li class="span4">
    <div class="thumbnail">
      <%= link_to wall, :class=>"img" do %>
        <%= image_tag wall.revisions.last.image.url(:square), :style=>"margin: 0 auto;" %>
      <% end %>
      <div class="caption">
          <h3><%= link_to wall.name, wall %>
            <% if wall.is_owned_by? @current_user %>
                <p class="controls">
                  <small>
                  <%= link_to 'Edit', edit_wall_path(wall) %>
                  <%= link_to 'Delete', wall, method: :delete, data: { confirm: 'Are you sure?' } %>
                  </small>
                </p>
            <% end %>
          </h3>

      </div>
    </div>
  </li>

  <% end %>
</ul>
